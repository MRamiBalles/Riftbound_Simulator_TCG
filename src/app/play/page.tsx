import { GameBoard } from '@/components/GameSimulator/GameBoard';

export default function PlayPage() {
    return (
        <main className="min-h-screen bg-black overflow-hidden">
            <GameBoard />
        </main>
    );
}
