apiVersion: ray.io/v1
kind: RayJob
metadata:
  name: riftbound-training-job-v2
  namespace: ray-system
spec:
  entrypoint: python /app/training/train_ppo.py
  rayClusterSpec:
    rayVersion: '2.9.0'
    headGroupSpec:
      replicas: 1
      rayStartParams:
        dashboard-host: '0.0.0.0'
      template:
        spec:
          containers:
          - name: ray-head
            image: manu/riftbound-ray-head:v1.0.0
            resources:
              limits:
                cpu: "2"
                memory: "16Gi"
              requests:
                cpu: "1"
                memory: "8Gi"
    workerGroupSpecs:
    - replicas: 4
      minReplicas: 2
      maxReplicas: 10
      groupName: cpu-group
      rayStartParams: {}
      template:
        spec:
          containers:
          - name: ray-worker
            image: manu/riftbound-ray-worker:v1.0.0
            resources:
              limits:
                cpu: "1"
                memory: "4Gi"
              requests:
                cpu: "500m"
                memory: "2Gi"
